# === Identificação ===
spring.application.name=ReiDosMotores_ITAM

# ==========================================
# CONFIGURAÇÃO NOVA (BANCO DA EMPRESA NA NUVEM)
# ==========================================

# Aqui estamos apontando para o site da empresa, na porta 3306, no banco que você criou
spring.datasource.url=jdbc:mysql://162.214.230.73:3306/reido702_itam?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# O usuário do cPanel (que aparece no seu print do phpMyAdmin)
spring.datasource.username=reido702

# AQUI VOCÊ COLOCA A SENHA QUE USA PARA ENTRAR NO PHPMYADMIN / CPANEL
spring.datasource.password=Rei@3164

# Driver (padrão)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==========================================
# CONFIGURAÇÃO ANTIGA (LOCAL - COMENTADA)
# (Tire os # se quiser voltar a usar o banco do seu PC)
# ==========================================
# spring.datasource.url=jdbc:mysql://localhost:3306/rei_itam?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=rei@motores

# ==========================================
# CONFIGURAÇÕES GERAIS (MANTENHA)
# ==========================================

# JPA / Hibernate (Isso vai criar as tabelas lá no phpMyAdmin sozinho)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Thymeleaf
spring.thymeleaf.cache=false

# ==========================================
# LIMITAR CONEXÕES (Crucial para Hospedagem Compartilhada)
# ==========================================
# O padrão é 10. Vamos baixar para 2 para não travar o banco da empresa.
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000